<!DOCTYPE html>
<html lang=<%= config.language === 'zh' ? 'zh-CN' : config.language %>>
<%- partial('_partial/head') %>
<body>

<div class="site-nav-toggle" id="site-nav-toggle">
    <button>
        <span class="btn-bar"></span>
        <span class="btn-bar"></span>
        <span class="btn-bar"></span>
    </button>
</div>

<div class="index-about">
    <i> <%= config.subtitle %> </i>
</div>

<div class="index-container">
    <% if(!is_current('/404')){%>
    <div class="index-left">
        <%- partial('_partial/nav', null, {cache: false}) %>
        <div class="index-about-mobile">
            <i> <%= config.subtitle %> </i>
        </div>
    </div>
    <% } %>
    <div class="index-middle">
        <!-- Main Content -->
        <%- body %>
    </div>
</div>

<%- partial('_partial/footer', null, {cache: false}) %>

<!-- codecopy support -->
<script type="text/javascript" src="/js/jquery.min.js"></script>
<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/clipboard@2.0.4/dist/clipboard.min.js"></script>
<script type="text/javascript" src="/js/clipboard.js"></script>

</body>

<script>
    // We expose some of the variables needed by the front end
    window.hexo_search_path = "<%- config.search ? config.search.path : '' %>"
    window.hexo_root = "<%- config.root %>"
    window.isPost = <%- is_post() %>
</script>
<%- js('js/index.js') %>
<script async src="//busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js"></script>
<script async src="/js/prism.js"></script>

<!-- 评论系统 -->
<% if (page.comments && config.comment) { %>
    <% if(config.comment.type === 'gitment') { %>
        <%- js('js/gitment.js') %>
        <script>
            var gitment = new Gitment({
                id: '<%- page.title %>',
                owner: '<%- config.comment.owner %>',
                repo: '<%- config.comment.repo %>',
                oauth: {
                    client_id: '<%- config.comment.id %>',
                    client_secret: '<%- config.comment.secret %>',
                },
            })
            gitment.render('comment-container')
        </script>
    <% } else if(config.comment.type === 'disqus') { %>
        <script>
            /**
            *  RECOMMENDED CONFIGURATION VARIABLES: EDIT AND UNCOMMENT THE SECTION BELOW TO INSERT DYNAMIC VALUES FROM YOUR PLATFORM OR CMS.
            *  LEARN WHY DEFINING THESE VARIABLES IS IMPORTANT: https://disqus.com/admin/universalcode/#configuration-variables
            */
            if( '<%- config.comment.url %>' || '<%- config.comment.identifier %>')
            var disqus_config = function () {
                this.page.url = '<%- config.comment.url %>';  // Replace PAGE_URL with your page's canonical URL variable
                this.page.identifier = '<%- config.comment.identifier %>'; // Replace PAGE_IDENTIFIER with your page's unique identifier variable
            };

            (function() { // DON'T EDIT BELOW THIS LINE
                var d = document, s = d.createElement('script');
                s.src = '<%- config.comment.script %>';
                s.setAttribute('data-timestamp', +new Date());
                (d.head || d.body).appendChild(s);
            })();
        </script>
    <% } else if(config.comment.type === 'giscus') { %>
        <%- config.comment.script %>
    <% } else if(config.comment.type === 'livere') { %>
        <script type="text/javascript">
            (function() {
                if (typeof LivereTower === 'function') { return; }

                var j, d = document.getElementById('lv-container');

                d.setAttribute('data-id','<%- config.comment.livere_id %>');
                d.setAttribute('data-uid' , '<%- config.comment.livere_uid %>');

                j = document.createElement('script');
                j.src = 'https://cdn-city.livere.com/js/embed.dist.js';
                j.async = true;

                d.appendChild(j);
            })();
        </script>
        <noscript>为正常使用来必力评论功能请激活JavaScript</noscript>
        </div>
    <% } %>
<% } %>

<!-- fancybox 支持 - 按需加载 -->
<% if(theme.fancybox === true && page.fancybox !== false) { %>
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // 只在需要时加载fancybox资源
        setTimeout(function() {
            var images = document.querySelectorAll('.index-middle img');
            if (images.length > 0) {
                var link = document.createElement('link');
                link.rel = 'stylesheet';
                link.href = '/js/fancybox/jquery.fancybox.min.css';
                document.head.appendChild(link);
                
                var script1 = document.createElement('script');
                script1.src = '/js/fancybox/jquery.fancybox.min.js';
                script1.onload = function() {
                    var script2 = document.createElement('script');
                    script2.src = '/js/fancybox/wrapImage.js';
                    document.body.appendChild(script2);
                };
                document.body.appendChild(script1);
            }
        }, 1000);
    });
</script>
<% } %>

</html>